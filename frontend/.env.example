# Environment example for frontend
# Copy to `.env` and fill values for your environment

# Base API URL (no trailing slash)
VITE_API_URL=

# WebSocket URL (ws:// or wss://)
VITE_WS_URL=

# Optional dev proxy target for Vite server (e.g. http://localhost:8081)
VITE_PROXY_TARGET=
