# Example application.properties (do NOT put real secrets here)
# Copy this to application.properties and fill values locally.

# Spring datasource example
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=your_db_user
spring.datasource.password=REDACTED

# Server port
server.port=8080

# Other local settings
#app.some.secret=REDACTED
